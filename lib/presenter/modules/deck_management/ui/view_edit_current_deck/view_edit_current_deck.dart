import 'package:flutter/material.dart';

class ViewEditCurrentDeck extends StatelessWidget {
  const ViewEditCurrentDeck({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('ViewMainEditCurrentDeck'),
      ),
      backgroundColor: Colors.brown[500],
      body: Container(),
    );
  }
}
