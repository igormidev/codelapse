import 'package:flutter/material.dart';

class ViewMainConfiguration extends StatelessWidget {
  const ViewMainConfiguration({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('ViewMainConfiguration'),
      ),
      body: Container(),
    );
  }
}
